# Gmail account used to notify Viola about registrations.
SMTP_USER=your-email@gmail.com
SMTP_APP_PASSWORD=replace-with-a-google-app-password

# Optional: set explicitly in production if the proxy forwards the client IP.
# RATE_LIMIT_TRUST_PROXY=true
